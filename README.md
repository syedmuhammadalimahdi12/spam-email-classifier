# spam-email-classifier
This project implements an Email Spam Classification system using Natural Language Processing (NLP) and Machine Learning techniques. It preprocesses text data, applies TF-IDF for feature extraction, and uses a Logistic Regression model with SMOTE to handle class imbalance.
